---
import { Footer } from '../components/Footer';
import Layout from './Layout.astro';

interface Props {
  title: string
}

const { title } = Astro.props;
---

<Layout title={title} header={{ transparent: true }}>
  <div>
    <main>
      <slot />
    </main>
    <Footer />
  </div>
</Layout>
